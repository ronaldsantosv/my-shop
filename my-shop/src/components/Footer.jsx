export default function Footer() {
  return (
    <footer className="container" style={{ padding: '1.5rem 0 2rem', color: '#475569' }}>
      <div className="d-flex justify-content-between flex-wrap gap-2">
        <span>Mi Shop — React + MockAPI</span>
        <small>Carrito protegido y CRUD completo.</small>
      </div>
    </footer>
  )
}
